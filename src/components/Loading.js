import React from 'react';
import loader from "../1487.gif";
const Loading = () => {
    return (
        <div>
            <img src={loader} alt='loading' />
            <h2> Loading </h2>
        </div>
    );
};

export default Loading;